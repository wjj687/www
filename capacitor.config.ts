import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.wjj687.shijing",
  appName: "\u8bd7\u7ecf",
  webDir: "out",
  android: {
    allowMixedContent: false
  }
};

export default config;
